# Step:1 Ubuntu (base image)
FROM ubuntu:latest

# Step:2 Nginx Install
RUN apt-get update && apt-get install -y -q nginx

# Step:3 file copy
COPY doc-goodple/index.htm /usr/share/nginx/html/

# Step:4 Nginx start
CMD ["nginx", "-g", "daemon off;"]
